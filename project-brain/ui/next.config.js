/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Если у вас есть кастомные настройки (rewrites, output, images и т.д.) —
    // перенесите их сюда в виде обычного объекта.
};

module.exports = nextConfig;
